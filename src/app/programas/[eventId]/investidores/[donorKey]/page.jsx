import DonorDetailPage from "../../doadores/[donorKey]/page";

// Alias route: we keep the underlying implementation in the legacy /doadores path,
// but expose the investor terminology in the URL as well.
export default DonorDetailPage;
